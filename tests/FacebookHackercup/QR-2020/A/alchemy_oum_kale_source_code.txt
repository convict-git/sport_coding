#include <iostream>
#include <bits/stdc++.h>
#include <map>
#include <cstring>
#include <utility>
#include <set>
#include <vector>
#define ll long long
using namespace std;

int main()
{
   ll t;
   cin>>t;
   ll C=1;
   while(t--)
   {
       ll n;
       cin>>n;
       string str;
       vector<string> ve;
       cin>>str;
       ll a = 0;
       ll b = 0;
       for(ll i=0;i<str.size();i++)
       {
  	    if(str[i]=='A')
             a++;
       }		
    if ((str.size() - 2 * a) ==1 || (str.size() - 2 * a)== -1 || (str.size() - 2 * a) ==0)
        cout << "Case #"<<(C++)<<": "<<"Y"<<endl;
    else
        cout << "Case #"<<(C++)<<": "<<"N"<<endl;
    /*       
for(ll i=2;i<n;i+=2)
       {	
           a = 0;
           b = 0;
           
           for(ll j=i-2; j<=i; j++)
           {
                if(str[j] == 'A')
                    a++;
                if(str[j] == 'B')
                    b++;     
           }	
          
               if(a>b)
                   str[i]='A';
               else
                   str[i]='B';
          
           if(i==n-1)
               break;
       }
 
       if(a==0 || b==0)
       {
             cout << "Case #"<<(C++)<<": "<<"N"<<endl;
       } 
       else
       {
             cout << "Case #"<<(C++)<<": "<<"Y"<<endl;
       }
  */
           
    }
    return 0;
}

